/* eslint-disable no-unused-vars */
// External imports
const axios = require('axios');

module.exports = {
  name: 'channelDelete',
  once: false,
  run: async (client, channel) => {
    // Do nothing
    return;
  },
};
