/* eslint-disable no-unused-vars */
// External imports
// const axios = require('axios');

module.exports = {
  name: 'guildBanRemove',
  once: false,
  run: async (client, ban) => {
    // console.log(ban);
  },
};
